# eq-sparks — the hidden agentic OS. The WHOLE .eq-sparks/ tree (synced harness + runtime
# cache/agent-memory/telemetry) is gitignored in every repo, central and consumer alike.
# Agents still READ these files on disk; they are just never committed.
.eq-sparks/

# Per-developer config (the .example is committed; the real one is not)
.eq-sparks.yml

# OS / editor noise
.DS_Store
*.log
node_modules/
